import string_operations as so

sample_string = "hello World"

print("Original:", sample_string)
print("Reversed:", so.reverse_str(sample_string))
print("Capitalized:", so.capitalize_str(sample_string))
print("Lowercase:", so.lowercase_str(sample_string))
print("Uppercase:", so.uppercase_str(sample_string))
